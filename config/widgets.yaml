# Widget configuration — tracked in git, no deploy required to update.

reading:
  title: "Designing Data-Intensive Applications"
  author: "Martin Kleppmann"
  cover_abbr: "DDIA"
  chapter: "ch. 7"
  started: "Apr 2026"
  total_pages: 590
  current_page: 342
  # Optional: a short phrase inside the quote to wrap in <em>
  quote: "The big idea behind encoded data is forward and backward compatibility."
  quote_highlight: "encoded data"

# Pin a specific project slug to the "Now Building" widget.
# Leave blank to auto-select the first project with status: wip.
featured_project:
  slug: ""

# Free-text entries for the "Currently" slab section.
# building and reading rows are derived from the featured project and reading widget above.
now:
  listening: "Latent Space podcast"
  climbing: "planning a Baker attempt this season"
  location: "Bellingham, WA · 48.7°N"
